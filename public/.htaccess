<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Skip the API proxy path if you ever add one.
  RewriteRule ^api/ - [L]

  # Serve existing files/dirs as-is.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
