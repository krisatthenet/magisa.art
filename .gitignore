node_modules
dist
.env
.env.local
server/data

.kilo
build.log
build.err
vite.log
*.stackdump

# local PocketBase runtime (binary + data), not part of the deployed app
pb/
logs/

# PocketBase schema migrations are required by the Railway image
!pb/
!pb/pb_migrations/
!pb/pb_migrations/*.js
